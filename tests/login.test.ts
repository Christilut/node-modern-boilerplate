import test from 'ava'
import * as httpStatus from 'http-status'
import app from '../config/express'

/**
 * This file tests every aspect of logging in. From succes to failures.
 */

test('test something', t => {
  t.deepEqual([1, 2], [3, 2])
})
